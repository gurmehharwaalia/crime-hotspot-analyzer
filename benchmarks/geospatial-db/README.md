# Geospatial DB Benchmark (PostGIS vs MongoDB)

This benchmark compares geospatial query performance using real databases.

## Requirements
- Docker
- Python 3

## How to Run

1. Start databases
```bash
docker compose up -d

